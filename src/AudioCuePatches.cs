// Audio cue system temporarily disabled pending asset investigation.
// Keep a no-op stub so the plugin still builds while this feature is offline.
namespace HypnagogiaAccess
{
    internal static class AudioCues
    {
        public static void Initialize()
        {
        }
    }
}
